# Driveby

Active release: [`v1.4.0`](./v1.4.0/) — the only version kept in the working tree.

Earlier releases (`v1.0.0`–`v1.3.0` and the `v0.x-beta` pre-Tauri snapshots) were removed from the tree; they remain in git history at commit `a768e06` and can be recovered with `git checkout a768e06 -- <version-folder>`.
